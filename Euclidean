def euclidean_distance(p1, p2):
    return math.sqrt(
        (p2[0] - p1[0])**2 +
        (p2[1] - p1[1])**2 +
        (p2[2] - p1[2])**2
    )

# Example:
a = (1, 2, 3)
b = (4, 6, 9)

print(euclidean_distance(a, b))
